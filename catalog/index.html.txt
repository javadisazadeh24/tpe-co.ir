<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <title>کاتالوگ روند</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://cdn.jsdelivr.net/npm/qrcode@1.5.1/build/qrcode.min.js"></script>
</head>
<body>
    <div class="catalog-grid" id="catalog-container"></div>
    <script>
        const projects = [
            { id: 'air-purifier', title: 'تصفیه هوا UV Plus', desc: 'فیلتراسیون دقیق' },
            { id: 'ecg-simulator', title: 'شبیه‌ساز ECG', desc: 'آناتومیک و دقیق' }
        ];
        const container = document.getElementById('catalog-container');
        projects.forEach(p => {
            container.innerHTML += `
                <div class="card" onclick="alert('مشاهده QR')">
                    <h3>${p.title}</h3>
                    <div id="qr-${p.id}"></div>
                </div>
            `;
            QRCode.toCanvas(document.getElementById(`qr-${p.id}`), p.id);
        });
    </script>
</body>
</html>